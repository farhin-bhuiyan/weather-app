<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Checker</title>
    <link rel="stylesheet" href="/css/styles.css"> <!-- Link to the CSS file -->
</head>
<body>
    <div class="container">
        <% if (willRain === null) { %> <!-- Check if there was an error -->
            <h1>Error: <%= error %></h1> <!-- Display the error message -->
        <% } else { %>
            <h1>Weather for <%= city %>:</h1> <!-- Display the city name -->
            <% if (willRain) { %>
                <p>Yes, it will rain tomorrow.</p> <!-- Display message if it will rain -->
            <% } else { %>
                <p>No, it will not rain tomorrow.</p> <!-- Display message if it will not rain -->
            <% } %>
        <% } %>
        <a href="/">Check another city</a> <!-- Link to go back to the home page -->
    </div>
</body>
</html>

